# ── 核心依赖 ──
PyQt6>=6.4.0
PyQt6-Fluent-Widgets>=1.5.0
httpx>=0.25.0
requests>=2.31.0

# ── 测试 ──
pytest>=8.0.0

# ── 编译 ──
nuitka>=2.0

# 构建依赖（打包 exe 时需要）
pyinstaller>=6.0
pyinstaller-hooks-contrib>=2024.0